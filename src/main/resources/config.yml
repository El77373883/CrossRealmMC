# Configuración del servidor Java (al que conectar internamente)
remote:
  address: "127.0.0.1"
  port: 25565

# Configuración del servidor Bedrock (para que los jugadores se conecten)
bedrock:
  address: "0.0.0.0"
  port: 19132

online-mode: false

bedrock-versions:
  - "*"

debug: true
